export const enum STORAGE_DATA {
    GEN_DATA = 'gen_data',
    SCAN_DATA = 'scan_data',
};

export const enum Paths {
    qr_scan = "/qr_scanner",
    qr_gen = "/qr_generator",
    hist_scan = "/history_of_scan",
    hist_gen = "/history_of_generate",
};